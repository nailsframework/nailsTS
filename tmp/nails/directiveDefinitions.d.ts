import { State } from "./state";
export declare class NailsDirectives {
    directives: any;
    constructor();
    form(element: HTMLElement, statement: string, state: State): void;
    for(element: HTMLElement, statemenet: string, state: State): void;
    if(element: HTMLElement, statement: string, state: State): void;
}
